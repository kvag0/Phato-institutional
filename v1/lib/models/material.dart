import 'package:flutter/material.dart';

// --- INSTRUÇÕES ---
// 1. Crie uma nova pasta `models` dentro de `lib`.
// 2. Crie um novo arquivo chamado `category.dart` dentro da pasta `lib/models/`.
// 3. Cole este código no novo arquivo.

class Category {
  final String name;
  final IconData icon;

  const Category({required this.name, required this.icon});
}
